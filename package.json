{
  "name": "talk-line",
  "version": "1.0.0",
  "description": "Our app features a straightforward and user-friendly interface, making it easy for users to understand and navigate. Each page is dynamically linked to MongoDB, ensuring seamless data management. The app is structured with the following pages:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon backend/server.js",
    "start": "node backend/server.js",
    "build": "npm install --legacy-peer-deps && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
